continue;用来提前结束本次循环，进入下一次循环，
	只能用在for、while、do-while三个循环语句中，与if条件语句一起来加速循环

break；用来跳出循环，提前终止循环


二者只对包含他们的最内层循环体语句起作用，对外层循环语句不起作用


如果程序想立即结束执行，那么continue和break就不那么适用了，
此时可用return语句直接退出函数，
或者使用exit函数终止程序执行（返回0暗示正常退出，其余为错误退出）

